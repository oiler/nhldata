[project]
name = "nhl-analytics"
version = "0.1.0"
description = "NHL game data analytics and timeline generation"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
    "anthropic>=0.40.0",
]

[tool.uv]
dev-dependencies = []
